# React Shop

A simple React shop for learning purposes.  
This project demonstrates the use of components, props, and basic styling in React.  
Product data is stored in a separate file — no API is used.

## Project Goals
- Practice creating and using React components
- Learn how to pass data through props
- Apply basic React styling

## Quick Start
1. Clone the repository
2. Install dependencies: `npm install`
3. Run the project: `npm start`
